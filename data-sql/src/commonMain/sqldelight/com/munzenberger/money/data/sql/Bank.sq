CREATE TABLE Bank (
    id TEXT NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    memo TEXT
);

selectAll:
SELECT id, name, memo FROM Bank;

insert:
INSERT INTO Bank (id, name, memo) VALUES (?, ?, ?);

update:
UPDATE Bank SET name = ?, memo = ? WHERE id = ?;

deleteById:
DELETE FROM Bank WHERE id = ?;
