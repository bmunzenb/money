CREATE TABLE Banks (
    id TEXT NOT NULL,
    name TEXT NOT NULL,
    memo TEXT,
    PRIMARY KEY (id)
);

selectAll:
SELECT * FROM Banks;

insert:
INSERT INTO Banks (id, name, memo) VALUES (?, ?, ?);

update:
UPDATE Banks SET name = ?, memo = ? WHERE id = ?;

deleteById:
DELETE FROM Banks WHERE id = ?;
